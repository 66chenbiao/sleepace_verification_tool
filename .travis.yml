os: linux
dist: focal
language: python
addons:
  apt:
    pkg:
      - python
#     packages:
#       - slp_utils
  
  
before_install:
  - python --version
  - pip install -U pytest
  - pip install pytest-cov
install:
#   - pip install ".[test]" . # install package + test dependencies
  
script: 
  - python3 -m pytest
  - pytest --cov=./ --cov-report=xml
# script: pytest # run tests

codecov:
  token: 99b7b1af-b962-4c5f-a63d-6d560709ed21

# token: 
#   - ${{ secrets.CODECOV_TOKEN }}

after_success:
  - codecov # submit coverage
